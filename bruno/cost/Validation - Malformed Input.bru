meta {
  name: Validation - Malformed Input
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/cost
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "input": "bad input"
  }
}

assert {
  res.status: eq 400
  res.body.error: isDefined
}

tests {
  test("should return 400 for unparseable input", function() {
    expect(res.getBody().error).to.be.a("string");
  });
}
